<div class="hidden md:flex md:flex-shrink-0">
    <div class="flex flex-col w-64 bg-slate-900">
        <div class="flex items-center h-16 px-4 bg-slate-800">
            <span class="text-xl font-bold text-white"><i class="fas fa-file-invoice-dollar mr-2"></i>CotizaPro</span>
        </div>
        <div class="flex flex-col flex-1 overflow-y-auto">
            <nav class="flex-1 px-2 py-4 space-y-1">
                <a href="/" class="sidebar-item flex items-center px-4 py-3 text-gray-300 hover:bg-slate-800 hover:text-white rounded-md transition duration-150">
                    <i class="fas fa-chart-line w-6"></i>
                    <span>Dashboard</span>
                </a>
                <a href="/cotizaciones" class="sidebar-item flex items-center px-4 py-3 text-gray-300 hover:bg-slate-800 hover:text-white rounded-md transition duration-150">
                    <i class="fas fa-file-invoice w-6"></i>
                    <span>Cotizaciones</span>
                </a>
                <a href="/clientes" class="sidebar-item flex items-center px-4 py-3 text-gray-300 hover:bg-slate-800 hover:text-white rounded-md transition duration-150">
                    <i class="fas fa-users w-6"></i>
                    <span>Clientes</span>
                </a>
                <div class="pt-4 mt-4 border-t border-slate-700">
                    <a href="/config" id="nav-config" class="sidebar-item flex items-center px-4 py-3 text-gray-300 hover:bg-slate-800 hover:text-white rounded-md transition duration-150">
                        <i class="fas fa-cog w-6"></i>
                        <span>Configuración</span>
                    </a>
                    <button onclick="auth.logout()" class="w-full flex items-center px-4 py-3 text-gray-300 hover:bg-red-900 hover:text-white rounded-md transition duration-150">
                        <i class="fas fa-sign-out-alt w-6"></i>
                        <span>Cerrar Sesión</span>
                    </button>
                </div>
            </nav>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const user = JSON.parse(localStorage.getItem('user') || '{}');
        if (user.rol !== 'ADMIN') {
            const navConfig = document.getElementById('nav-config');
            if (navConfig) navConfig.classList.add('hidden');
        }
    });
</script>





