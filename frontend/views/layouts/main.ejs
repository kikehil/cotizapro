<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CotizaPro - Sistema de Cotizaciones</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        [v-cloak] { display: none; }
        .sidebar-item-active { background-color: #3b82f6; color: white; }
        
        /* Sidebar transition */
        #sidebar-container {
            transition: all 0.3s ease-in-out;
            z-index: 50;
        }
        .sidebar-collapsed #sidebar-container {
            margin-left: -16rem; /* 64 * 4px */
        }
        #sidebar-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 40;
        }
        .sidebar-open #sidebar-overlay {
            display: block;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans antialiased">
    <div class="flex h-screen overflow-hidden" id="app-layout">
        <!-- Sidebar Overlay -->
        <div id="sidebar-overlay" onclick="ui.toggleSidebar()"></div>

        <!-- Sidebar -->
        <div id="sidebar-container" class="flex-shrink-0 w-64">
            <%- include('../components/sidebar') %>
        </div>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden" onclick="ui.closeSidebarIfOutside(event)">
            <!-- Header -->
            <%- include('../components/header') %>

            <!-- Content -->
            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100 p-6">
                <%- body %>
            </main>
        </div>
    </div>

    <script src="/js/auth.js"></script>
    <script src="/js/common.js"></script>
</body>
</html>







